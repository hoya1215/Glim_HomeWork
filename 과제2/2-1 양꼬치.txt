#include <string>
#include <vector>

using namespace std;

int nSheepCost = 12000;
int nDrinkCost = 2000;

int solution(int n, int k) {

    int nAnswer = nSheepCost * n + nDrinkCost * k;
    nAnswer -= nDrinkCost * (n / 10);
    
    return nAnswer;
}